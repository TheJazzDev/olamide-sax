# Database
# PostgreSQL connection string for NeonDB or any PostgreSQL database
# Example: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://..."

# Node Environment (automatically set by Vercel)
NODE_ENV="production"
